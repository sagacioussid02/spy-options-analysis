"""Parked experimental modules — NOT part of the decision path.

These were moved out of `utils/` because nothing in `main.py` or the engines
imports them: they computed numbers/reports that never reached the actual
BUY/SELL decision. They're kept (not deleted) so you can revisit or revive them,
but they no longer add complexity to the live decision logic.

See README.md in this folder for what each one was.
"""
